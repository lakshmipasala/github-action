name: Env demo
on: workflow_dispatch
env:
  env_demo: "dev"
jobs:
  envdemo:
    env:
      env_demo: "test"
    runs-on: ubuntu-latest
    steps:
      - name: test env demo
        env:
          env_demo: "prod"
        run: echo "$env_demo"
