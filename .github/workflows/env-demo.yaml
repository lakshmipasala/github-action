name: Env demo
on: workflow_dispatch
env:
  env_demo: "dev"
jobs:
  env:
    env_demo: "test"
  envdemo:
    runs-on: ubuntu-latest
    steps:
      - name: test env demo
        run: echo "$env_demo"
