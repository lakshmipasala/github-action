name: Hello-world
on: workflow_dispatch
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps: 
      - name: hello step
        run: |
          echo " this is line one"
          echo " this is line two"
